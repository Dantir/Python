f = open('data.txt','w')
text = 'something'
while (text!='exit'):
    text = input("What do you want to add to the file? (to exit write 'exit')\n")
    if(text!='exit'):
        f.write(text)
f.close()
